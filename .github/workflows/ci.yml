name: CI-Quality-Gate

on:
  push:
    branches: [main, develop, "feature/*"]
  pull_request:
    branches: [main, develop]

jobs:
  lint-and-security:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install Linting Tools
        run: pip install ruff bandit

      - name: Run Ruff Check (Linter + Isort)
<<<<<<< HEAD
        run: ruff check backend/src/ --exclude backend/src/generated

      - name: Run Security Scan (Bandit)
        run: bandit -r backend/src/ -x backend/src/generated -ll
=======
        run: ruff check src/

      - name: Run Security Scan (Bandit)
        run: bandit -r src/ -ll

  test:
    runs-on: ubuntu-latest
    needs: lint-and-security  # ← só roda testes se o lint passar

    services:
      postgres:
        image: ankane/pgvector:latest
        env:
          POSTGRES_USER: admin
          POSTGRES_PASSWORD: admin
          POSTGRES_DB: superdott_test   # banco SEPARADO para testes
        ports:
          - 5432:5432
        options: >-
          --health-cmd "pg_isready -U admin -d superdott_test"
          --health-interval 5s
          --health-timeout 5s
          --health-retries 5

    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: pip install -r requirements.txt

      - name: Run Tests
        env:
          GEMINI_API_KEY: "test_key_fake_para_ci"
          DATABASE_URL: "postgresql://admin:admin@localhost:5432/superdott_test"
          TEST_DATABASE_URL: "postgresql://admin:admin@localhost:5432/superdott_test"
          ENV: "test"
        run: pytest tests/ -v
>>>>>>> origin/develop
